[project]
name = "p2-sam"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "faker",
    "sqlalchemy",
    "pyyaml",
    "pgeocode",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[tool.setuptools.packages.find]
include = ["p2_sam*"]